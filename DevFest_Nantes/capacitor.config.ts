import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.diginamic.app',
  appName: 'DevFest_Nantes',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
